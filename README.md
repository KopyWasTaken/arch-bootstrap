# Dotfiles

Slowly but surely ripping configs from all around the internet to make my own.

Make sure to rename this folder to `.dotfiles` otherwise the paths will be
broken.

This should be easy to setup using GNU Stow:

```bash
stow .
```
